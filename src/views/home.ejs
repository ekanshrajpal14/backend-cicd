<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Blog Platform | Share Your Stories</title>

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
    }

    body {
      background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
      color: #ffffff;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    header {
      padding: 20px 40px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      background: rgba(0, 0, 0, 0.2);
    }

    header h1 {
      font-size: 22px;
      font-weight: 600;
      letter-spacing: 1px;
    }

    nav a {
      color: #ffffff;
      text-decoration: none;
      margin-left: 20px;
      font-size: 14px;
      opacity: 0.9;
    }

    nav a:hover {
      opacity: 1;
      text-decoration: underline;
    }

    main {
      flex: 1;
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
      padding: 40px 20px;
    }

    .hero {
      max-width: 700px;
    }

    .hero h2 {
      font-size: 48px;
      margin-bottom: 20px;
      line-height: 1.2;
    }

    .hero p {
      font-size: 18px;
      opacity: 0.9;
      margin-bottom: 30px;
    }

    .hero button {
      background: #ffffff;
      color: #2c5364;
      border: none;
      padding: 14px 32px;
      font-size: 16px;
      border-radius: 30px;
      cursor: pointer;
      font-weight: 600;
      transition: all 0.3s ease;
    }

    .hero button:hover {
      background: #e0e0e0;
    }

    footer {
      text-align: center;
      padding: 15px;
      font-size: 13px;
      background: rgba(0, 0, 0, 0.2);
      opacity: 0.8;
    }

    @media (max-width: 600px) {
      .hero h2 {
        font-size: 34px;
      }

      .hero p {
        font-size: 16px;
      }
    }
  </style>
</head>
<body>

  <header>
    <h1>Blog Platform</h1>
    <nav>
      <a href="#">Home</a>
      <a href="#">Blogs</a>
      <a href="#">Login</a>
      <a href="#">Register</a>
    </nav>
  </header>

  <main>
    <div class="hero">
      <h2>Share Your Stories with the World</h2>
      <p>
        A modern blogging platform to write, publish, and explore meaningful
        content from creators around the globe.
      </p>
      <button>Get Started</button>
    </div>
  </main>

  <footer>
    © <%= new Date().getFullYear() %> Blog Platform. All rights reserved.
  </footer>

</body>
</html>
